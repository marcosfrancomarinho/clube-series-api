export type IStructureFooter = { id: number; url: string; redes: string };

export type IStructureMenu = { id: number; public: string[]; private: string[]; title: string };

export type IStructureImages = { id: number; url: string; title: string };
